number = int(input("Enter a number: ", ))
count = 0

while number > 1:

    if number % 2 == 1:
        number = number * 3 + 1

    else:
        number = number / 2
    print(number)
    count = count + 1

print("Number of steps it takes for the Collatz orbit to reach 1 is:", count)
